version: '3.8'

services:
  app:
    build:
      context: ./vulnerable
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - JWT_SECRET=demo-secret-key-not-for-production
