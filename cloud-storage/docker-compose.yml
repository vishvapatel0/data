version: '3.8'

services:
  vulnerable:
    build:
      context: ./vulnerable
      dockerfile: Dockerfile
    ports:
      - "8083:8080"
    environment:
      - JWT_SECRET=demo-secret-key-not-for-production

  fixed:
    build:
      context: ./fixed
      dockerfile: Dockerfile
    ports:
      - "8084:8080"
    environment:
      - JWT_SECRET=demo-secret-key-not-for-production
