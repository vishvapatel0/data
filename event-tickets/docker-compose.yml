version: '3.8'

services:
  app:
    build:
      context: ./app
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - APP_KEY=base64:your-app-key-here
      - JWT_SECRET=demo-secret-key-not-for-production
