name: CI

on:
  push:
    branches: [main]
    paths:
      - 'healthcare-records/**'
  pull_request:
    branches: [main]
    paths:
      - 'healthcare-records/**'

jobs:
  test-vulnerable:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: healthcare-records/vulnerable

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

  test-fixed:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: healthcare-records/fixed

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

  security-scan:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: healthcare-records/vulnerable

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Run npm audit
        run: npm audit || true
